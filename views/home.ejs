<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Members Only</title>
</head>
     <style>
        body{
            font-family: arial;
            margin: 0;
        }
        .headerContainer{
            display: flex;
            justify-content: space-between;
            background: #50114d;
            padding: 20px 30px;
            position: sticky;
            top: 0;
        }
        nav {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        nav a {
            color: #fff;
            font-size: 18px;
            background: rgba(255, 255, 255, 0.2);
            text-decoration: none;
            padding: 10px;
            border-radius: 5px;
        }
        nav a:hover {
            background-color: rgba(255, 255, 255, 0.4);
        }
        h1 {
            color: rgb(110, 189, 199);
        }
        main , header{
            max-width: 960px;
            margin: 30px auto;
            padding: 0 10px;
            color: #333;
        }
        .messagesDiv{
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .main-header {
            color: #333;
            text-align: center;
        }
        .note {
            text-align: center;
            color: #6b6262;
            font-style: italic;
            text-decoration: underline;
        }
        .message-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: #fff;
            margin: 10px;
            padding: 10px;
            border-radius: 5px;
            box-shadow: 0 0 5px 0 rgba(0, 0, 0, 0.1);
        }
        .message-list {
            list-style: none;
            padding: 0 2%;
        }
        .message-text {
            color: #333;
            margin: 0;
        }
        .user-heading-text {
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            margin-bottom: 5px;
        }
        .message-user {
            color: hsl(90, 1%, 27%);
            margin-bottom: 10px;
        }
        .message-time {
            color: #888;
            margin: 0;
        }
     </style>
<body>
    <div class="headerContainer">
        <h1>Members only</h1>
        <nav>
            <p class="link"><a href="/auth/login">Log In</a></p>
            <p class="link"><a href="/auth/sign-up">Sign Up</a></p>
        </nav>
    </div>
    <div class="messagesDiv">
        <h1 class="main-header">
            <%= title %>
        </h1>
        <p class="note">Note: You must be a member to view the post's author and date of creation.</p>
        <div class="message-list">
            <% messages.forEach(function(message) { %>
                <div class="message-container">
                    <p class="message-text">
                        <%= message.message %>
                    </p>
                    <div class="user-heading-text">
                        <p>Post by: <strong>#######</strong></p>
                        <p class="message-time">Date: <em> #######</em></p>
                    </div>
                </div>
                <% }); %>
        </div>
    </div>
</body>
</html>